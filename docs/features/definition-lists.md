# Definition lists

A term on one line, followed by one or more `:` definition lines (PHP-Markdown-Extra /
Pandoc style). Terms render in bold, definitions indented; they round-trip to Markdown.

```markdown
Apple
: A fruit
: A technology company

Markdown
: A lightweight markup language
```
