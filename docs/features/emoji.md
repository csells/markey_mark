# Emoji shortcodes

Type a shortcode like `:rocket:` and it becomes the emoji 🚀. Unknown shortcodes are left as
plain text. Emoji round-trip as their Unicode glyph.

```markdown
Ship it :rocket: — I :heart: Flutter
```
